package datastructures.stack;

public class MyStackWithArray{

    // fields -- this will internally have an array
    int array[]; // initially it will have 10 elements

    public MyStackWithArray(int initialSize){
        array = new int[initialSize];
    }

    // methods: generally stack methods are of the format: push, pop, top, peek

    // push
    public void push(int data){

    }

    // pop
    public int pop(){
        return 0;
    }

    // peek
    public int peek(){
        return 0;
    }

    // top
    public int top(){
        return 0;
    }

}