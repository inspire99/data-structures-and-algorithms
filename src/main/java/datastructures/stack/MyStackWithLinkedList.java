package datastructures.stack;

import java.util.LinkedList;

public class MyStackWithLinkedList{

    // fields: let's keep a linkedlist containing int field
    LinkedList<Integer> linkedList;

    // should i use array to implement stack or linkedlist to implement stack?

    // let's try linkedlist approach here.

    // constructor

    public MyStackWithLinkedList(int initialSize){
        //linkedList = new LinkedList<>(initialSize);
    }

    // methods: add, insert?, remove?
    // generally stack methods are of the formats: pop, push, top, peek

    // pop
    public int pop(){

        return 0;
    }

    //push
    public void push(int data){

    }

    //top
    public int top(){
        return 0;
    }

    //peek
    public int peek(){
        return 0;
    }

}